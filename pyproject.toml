[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-export"
version = "0.1.0"
description = "Export tiles to JSON, CSV, Markdown, and compact formats"
license = {text = "MIT"}
requires-python = ">=3.8"
